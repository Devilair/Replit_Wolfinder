{
  "name": "@wolfinder/server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "dev": "tsx -r tsconfig-paths/register index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@wolfinder/shared": "workspace:*",
    "@wolfinder/shared-types": "workspace:*"
  }
} 